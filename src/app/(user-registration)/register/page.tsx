import RegisterForm from "@/features/auth/components/auth/register/RegisterForm";

type Props = {};

const page = (props: Props) => {
  return (
    <>
      <RegisterForm />
    </>
  );
};

export default page;
